# For local backend connecting to published Postgres port
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@127.0.0.1:5432/compliance

# For backend run inside Docker Compose with internal network
DATABASE_URL=postgresql+psycopg2://postgres:postgres@postgres:5432/compliance
